issues:
  exclude-rules:
    # resistor.wasm and wasm_exec.js are generated by make build-wasm and
    # are not committed to the repo, so go vet cannot find them at lint time.
    - path: web/embed\.go
      linters:
        - govet
