version: "2"

issues:
  exclusions:
    rules:
      # resistor.wasm and wasm_exec.js are generated by make build-wasm and
      # are not committed, so the embed pattern cannot be resolved at lint time.
      - linters:
          - govet
          - typecheck
        text: "pattern resistor.wasm: no matching files found"
