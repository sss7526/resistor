issues:
  exclude-rules:
    # resistor.wasm and wasm_exec.js are generated by make build-wasm and
    # are not committed, so go vet cannot find them at lint time.
    - path: web/embed\.go
      linters:
        - govet
        - typecheck
